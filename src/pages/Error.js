import React from 'react';

const Error = () => {
    return (
        <div>
            {/* Display an error message when an invalid route is accessed */}
            <h2>Oops. Error Page</h2>
        </div>
    );
};

export default Error;
